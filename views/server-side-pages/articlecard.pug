extends index.pug

block title
  = article.name

block maincontent
  .article-card
    .article-text-wrapper
      img(src= '/resources/images/articles/' + article.imageName ng-src='{{utils.getArticleImageUrl(article)}}')
      h1= article.name
      p(ng-bind-html='article.description')!= article.description
